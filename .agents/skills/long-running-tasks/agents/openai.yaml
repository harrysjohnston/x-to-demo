interface:
  display_name: "Long-Running Tasks"
  short_description: "Run durable multi-step tasks safely"
  default_prompt: "Use $long-running-tasks to plan, execute, checkpoint, and close out a long-running task with bounded loops and resumable state."
